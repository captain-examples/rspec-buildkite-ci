steps:
  - name: ":rspec:"
    command: |
      pwd
      ls -la
      ls -la .buildkite/**

      which sh

      .buildkite/steps/rspec.sh

    plugins:
      docker-compose#v4.11.0:
        run: app
